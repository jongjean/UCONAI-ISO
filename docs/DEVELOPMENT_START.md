# ISO Development Start

Date: 2026-05-03

Development has started as source scaffolding only.

This step intentionally does not:

- Create `/uconai/data/iso`.
- Create or migrate a DB.
- Build to `/uconai/www/iso`.
- Change Caddy.
- Start or restart a service.
- Connect a paid external AI API.

The first implementation target is now complete enough for Phase 0. The next target is Phase 1 in `docs/00_MASTER_EXECUTION_MAP.md`: rule IDs, API contracts, schema review and contract tests before persistent writes.


